version: '1'
services:
  backend:
    build: ./back-end
    container_name: backend
    ports:
      - "8000:8000"
  frontend:
    build: ./front-end
    container_name: frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend